namespace InternalWiki.Data
{
    public class BluePrint
    {
        //TODO: Method to write text file.
        public static bool WriteTextFile()
        {
            using system.file.io {
                open the file
                write the html into the file
            }implicit close of the file 
        if op success return true
        else return false    
        }
        
        //TODO: Method to read a file.
        public static string ReadTextFile()
        {
           using System.file.iO{
              open the file
                  read all text
            } implicit close of file
           return text
        }
        
        //TODO: Takes raw text and displays it using TS.
        public static bool FrontEndProcess()
        {
           take raw string and add it's top part to the title, the middle part to the <a> tags,
               and the rest of it to the keywords list/part. 
            
        }
        //TODO: Create a generic typescript form for article processing that can be used on every page.
        //This template should have an input form, a few buttons.
        //We can use this to create and modify data.
         
        
    }
}
